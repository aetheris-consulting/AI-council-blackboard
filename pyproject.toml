[project]
name = "council-blackboard-mcp"
version = "0.1.0"
description = "FastMCP/Horizon deployment surface for the Council Blackboard System"
requires-python = ">=3.10"
dependencies = [
  "fastmcp==4.0.3",
]

[tool.uv]
package = false
